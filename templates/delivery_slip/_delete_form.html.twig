<form method="post" action="{{ path('delivery_slip_delete', {'id': delivery_slip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_slip.id) }}">
    <button class="btn">Delete</button>
</form>
