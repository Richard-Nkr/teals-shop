{% extends 'base.html.twig' %}

{% block title %}Live{% endblock %}

{% block body %}
    <h1>Live</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ live.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ live.dateDebut ? live.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ live.dateFin ? live.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index') }}">Voir les commandes</a>

    <a href="{{ path('live_edit', {'id': live.id}) }}">edit</a>

    {{ include('live/_delete_form.html.twig') }}
{% endblock %}
